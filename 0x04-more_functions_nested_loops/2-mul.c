#include "main.h"

/**
 * mul - it multiply two numbers
 * @a: first parameter
 * @b: second parameter
 * Return: result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
