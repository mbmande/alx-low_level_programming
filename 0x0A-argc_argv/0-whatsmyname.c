#include <stdio.h>

/**
 * main - it returns the file name
 * @argc: number of argument
 * @argv: argument holder
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
